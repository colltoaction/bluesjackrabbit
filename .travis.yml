sudo: required
language: cpp
compiler: gcc
os: linux

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y libgtkmm-3.0-dev libcppunit-dev

install:
  - mkdir build
  - cd build
  - cmake ../
  - make

script:
  - make test
